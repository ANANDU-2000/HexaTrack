# Import Export Rules

CSV import must support preview, validation, duplicate detection, invalid row handling, timezone handling, and rollback.

Exports must be user-initiated, privacy-safe, rate-limited, and audited.
