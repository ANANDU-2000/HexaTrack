# Form Patterns

- Validate on blur and submit.
- Disable submit while saving.
- Prevent double submit.
- Amount fields use numeric keypad and decimal rules.
- Currency formatting must not corrupt stored decimal values.
- Sticky submit stays above keyboard.
