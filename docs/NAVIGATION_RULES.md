# Navigation Rules

- Bottom nav max 3 primary tabs unless explicitly approved.
- Add Transaction can be a central action/sheet.
- Modal vs page must match task depth.
- Back button must close sheets before leaving screen.
- Avoid nested tabs and deep stacks.
