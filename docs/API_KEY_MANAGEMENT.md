# API Key Management

- Raw provider keys are backend-only.
- UI shows masked status only.
- Store secrets in hosting secret manager or encrypted storage.
- Rotate and revoke keys with audit trail.
- AI provider/model switching must not require frontend exposure or redeploy.
