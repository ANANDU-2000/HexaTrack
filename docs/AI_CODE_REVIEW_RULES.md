# AI Code Review Rules

Forbidden: nested business logic, duplicated validation, random Tailwind, magic numbers, untyped responses, unbounded queries, N+1 queries, giant components, UI-to-DB shortcuts.

Required: service layer, DTO validation, typed API, pagination, workspace scoping, idempotency, loading/error/empty states, tests, accessibility, mobile validation.
