# DB Scaling Rules

- Index workspace-scoped foreign keys.
- Cursor paginate history.
- Cache dashboard/report aggregates where safe.
- Review slow queries with EXPLAIN.
- Avoid N+1 queries.
- Consider partitioning transaction history only when data volume requires it.
