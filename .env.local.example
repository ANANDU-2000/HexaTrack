# HexaTrack frontend — copy to .env.local (never commit .env.local)
# Vercel: set the same NEXT_PUBLIC_* vars in Project → Settings → Environment Variables

NEXT_PUBLIC_API_BASE_URL=https://hexatrack.onrender.com
NEXT_PUBLIC_APP_NAME=HexaTrack

# Payments (optional until billing is wired)
NEXT_PUBLIC_RAZORPAY_KEY_ID=
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=
